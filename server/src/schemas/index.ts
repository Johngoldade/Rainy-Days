// import typedefs and resolvers to a common location and then export them for use by the server
import typeDefs from './typeDefs.js';
import resolvers from './resolvers.js';

export { typeDefs, resolvers };
